{
  "name": "vscode-dock-phase0-probe",
  "displayName": "Dock Phase 0 Probe (throwaway)",
  "description": "Disposable probe: logs window identity + remote authority to settle the extensionKind placement (Q1) and windowId source (Q2) questions. Delete after Phase 0.",
  "version": "0.0.0",
  "private": true,
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "//extensionKind": "Probing 'ui' placement: does a UI-host extension see the wsl+ authority? Flip to ['workspace'] and re-run to compare.",
  "extensionKind": ["ui"],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "dockProbe.dump",
        "title": "Dock Probe: Dump window identity again"
      },
      {
        "command": "dockProbe.writeTestFile",
        "title": "Dock Probe: Write a test file to the shared dir"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
