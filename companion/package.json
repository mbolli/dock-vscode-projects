{
  "name": "vscode-projects-dock-companion",
  "displayName": "VS Code Projects Dock — Companion",
  "description": "Silent reporter: publishes this window's identity and liveness to the shared state directory the PowerToys dock reads. Reports what is, never what to do.",
  "version": "0.1.0",
  "publisher": "zweiundeins",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "// extensionKind": "Phase 0 confirmed a ui-host extension sees the wsl+ authority and writes natively to %LOCALAPPDATA%.",
  "extensionKind": ["ui"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscodeProjectsDock.showStatus",
        "title": "VS Code Projects Dock: Show companion status"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
